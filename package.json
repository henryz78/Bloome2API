{
  "name": "model-gateway",
  "version": "1.0.0",
  "description": "OpenAI-compatible model API gateway with multi-protocol request translation and sanitized public errors.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "node --test test/*.test.mjs",
    "check": "node --experimental-strip-types --check src/index.ts"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0"
  }
}
